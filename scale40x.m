function length_per_pixel = scale40x() 

length_per_pixel = 1.841620626151013e-07;

% Roughly based on this, though the exact number was refined manually
% scale_image = imread('e:/brandon/microscope_images/Scale_40x.jpg');
% scale_width_meters = 380e-6;
% scale_width_pixels = size(scale_image,2);
% length_per_pixel = scale_width_meters/scale_width_pixels;